// camelCase
// snake_case
// PascalCase

let userName: string = 'Junaid' // e.g. camelCase
let user_name: string = 'Maaz' // e.g. snake_case
let UserName: string = 'Chota Bheem' // e.g. PascalCase

let isUserExist: boolean = true;
let is_user_exist: boolean = false;
let IsUserExist: number = 1;
 
// = is assignment operator

// console.log(userName, user_name, UserName)
// console.log(isUserExist, is_user_exist,IsUserExist)

// Arrays => [] => 0, ...

let students = ["Junaid", "Maaz", "Kamran", 1, 10, true]
let shapatar = students[1]

console.log(shapatar)
console.log(students[5])
console.log(students[6])
